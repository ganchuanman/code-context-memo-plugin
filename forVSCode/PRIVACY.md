# Privacy

Code Context Memo stores memo data locally in VS Code workspace state and stores AI settings in VS Code global state.

API keys are stored through VS Code SecretStorage.

The extension does not include telemetry.

AI features are optional. If you use them, the current memo and task background are sent to the OpenAI-compatible endpoint you configure.
