# Privacy

Code Context Memo stores memo data locally in the JetBrains project workspace file and stores AI settings at IDE application level.

API keys are stored through JetBrains Password Safe.

The plugin does not include telemetry.

AI features are optional. If you use them, the current memo and task background are sent to the OpenAI-compatible endpoint you configure.
